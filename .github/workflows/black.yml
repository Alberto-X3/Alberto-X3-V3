name: Lint

on:
  push:
    branches: [ stable, develop ]
  pull_request:
    branches: [ stable, develop ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
